/** @deprecated Import from `./katex` instead. Kept for existing paths. */
export {
  observeMathAutoRender,
  renderBookMath,
  renderLatex,
  renderMathInContainer,
} from "./katex";
export type {
  RenderBookMathOptions,
  RenderLatexOptions,
  RenderMathInContainerOptions,
} from "./katex";
